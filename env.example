# API Gateway URL
# For development: http://localhost:3000
# For production: https://api.yoursite.com (or your actual gateway URL)
VITE_API_GATEWAY=http://localhost:3000

# Use microservice for spin results (server-side control)
# Set to 'true' to enable server-side spin results (recommended for production)
# Set to 'false' to use client-side spinning (default for development)
VITE_USE_MICROSERVICE_FOR_SPIN=false

